# Cloud Run service template (reference). Deploy with scripts/deploy-cloudrun.sh
# which applies env from .env.prod and runs: gcloud run deploy --source .
#
# Cost guardrails (Always Free region us-east1):
#   min-instances: 0, max-instances: 1, memory: 512Mi, cpu-throttling, no cpu-boost
#
# apiVersion: serving.knative.dev/v1
# kind: Service
# metadata:
#   name: salvai-be
#   annotations:
#     run.googleapis.com/ingress: all
# spec:
#   template:
#     metadata:
#       annotations:
#         autoscaling.knative.dev/minScale: "0"
#         autoscaling.knative.dev/maxScale: "1"
#         run.googleapis.com/cpu-throttling: "true"
#     spec:
#       containerConcurrency: 80
#       timeoutSeconds: 300
#       containers:
#         - image: REGION-docker.pkg.dev/PROJECT/REPO/salvai-be:latest
#           ports:
#             - containerPort: 8080
#           env:
#             - name: WEB_CONCURRENCY
#               value: "1"
#           resources:
#             limits:
#               cpu: "1"
#               memory: 512Mi
